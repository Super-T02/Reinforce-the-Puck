
\subsection{Hybrid-Model}\label{sec:hybrid}
While reviewing the gameplay of the trained \ac{SAC} and \ac{TD3} agents,
we noticed that both agents have their strengths and weaknesses in different situations.
To combine the strengths of both agents, we created a hybrid model.
This approach is inspired by the \ac{MoE} architecture, where multiple experts are combined using a gating network to weight the experts predictions.
But since we've got a continuous spatial action space, weighting both experts predictions would not be a feasible option.
Thus, a \ac{DDQN} \cite{vanhasselt2015deepreinforcementlearningdouble} was implemented as a selection gate for sub-agents.
This gate estimates the value of choosing a specific sub-agent, given a state:

\begin{gather}
    i^*(s) = \arg\max_{i \in \text{Sub-Agents}} Q(i, s) \\
    a = \pi_{i^*(s)}(s)
\end{gather}

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}[auto,>=latex']
      % Definieren der benutzerdefinierten Farben
      \definecolor{statecolor}{RGB}{173,216,230}    % Light Blue
      \definecolor{diamondcolor}{RGB}{255,182,193}    % Light Pink
      \definecolor{saccolor}{RGB}{255,255,224}          % Light Yellow
      \definecolor{td3color}{RGB}{224,255,255}          % Light Cyan
      \definecolor{actioncolor}{RGB}{255,228,181}       % Moccasin

      % Positionierung der Knoten
      % Zustand s
      \node [draw, rectangle, fill=statecolor, minimum width=2cm, minimum height=1cm, rounded corners] (state) at (0,0) {State $s$};

      % Entscheidungs-Diamond: argmax mit erhöhter Größe für längeren Text
      \node [draw, diamond, fill=diamondcolor, aspect=3, inner sep=2pt, minimum width=3cm] (argmax) at (4.5,0)
        {$\arg\max\limits_{i \in \text{Sub-Agents}} Q(i,s)$};

      % Sub-Agents, horizontal angeordnet: SAC oben, TD3 unten
      \node [draw, rectangle, fill=saccolor, minimum width=3.5cm, minimum height=1cm, rounded corners] (sac) at (8,1.5)
        {\shortstack{SAC Agent \\ $\pi_{\text{SAC}}(s)$}};
      \node [draw, rectangle, fill=td3color, minimum width=3.5cm, minimum height=1cm, rounded corners] (td3) at (8,-1.5)
        {\shortstack{TD3 Agent \\ $\pi_{\text{TD3}}(s)$}};

      % Ausgabe: Action a
      \node [draw, rectangle, fill=actioncolor, minimum width=3.5cm, minimum height=1cm, rounded corners] (action) at (12,0)
        {Action $a$};

      % Pfeile
      \draw[->, thick] (state) -- (argmax);
      \draw[->, thick] (argmax) -- node [above left] {$i^*(s)=\text{SAC}$} (sac);
      \draw[->, thick] (argmax) -- node [below left] {$i^*(s)=\text{TD3}$} (td3);
      \draw[->, thick] (sac) -- (action);
      \draw[->, thick] (td3) -- (action);
    \end{tikzpicture}
    \caption{Hybrid model with \ac{DDQN}-based agent gate using \ac{SAC} and \ac{TD3} as sub-agents.}
  \end{figure}


The loss function for training the \ac{DDQN} is based on the \ac{TD}-Error,
which is the difference between the predicted and the target Q-value. We investigated three different training strategies for the hybrid model:
\begin{itemize}
    \item Training from scratch:  Training the subagents and the gating network simultaneously from scratch.
    \item Training by fine-tuning the sub-agents: Using sub-agents that are pre-trained independently and further finetuned simultaneously to the gating network.
    \item Training with fixed sub agents: Using subagents that are pre-trained independently and left fixed while training the gating network.
\end{itemize}
