\subsection{Hockey Environment} \label{subsec:hockey_eval}

The performance on the hockey environment is evaluated with the average final performance (\autoref{tab:avg_reward}) and the corresponding evaluation reward of the training in 600000 environment steps (\autoref{fig:eval-own-reward}). Therefore, we train \ac{SAC} and \ac{TD3} on the original environment reward and our own reward. The opponents are the strong/weak basic opponents, \ac{SAC} foundation, \ac{TD3} foundation and Hybrid foundation agents, which are pretrained on the strong/weak opponents. To stabalize training, the opponents change after 10000 environment steps (100 epochs). The evaluation reward contains only the win/lose signal with $r_{eval} \in \left\{-10, 0, 10\right\}$ and is recorded every 5000 steps (50 epochs) over 20 runs. The average reward is then saved and plotted in \autoref{fig:eval-own-reward}. All other statistics, like critic-, actor-loss and training reward provided in the appendix \autoref{sec:hockey-plots}. The final average evaluation reward over 100 runs is also provided in \autoref{tab:avg_reward}.


\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/baseline_Hockey_EvalReward.png}
    \caption{Evaluation Rewards for \ac{SAC} and \ac{TD3} with original and own reward. \ac{TD3} is also presented with \ac{BER} and \ac{PER} over 600000 environment steps.}
    \label{fig:eval-own-reward}
\end{figure}


\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/selfplay_rewards_eval.png}
    \caption{Selfplay Rewards Evaluation for \ac{SAC}, \ac{TD3} and the Hybrid model.}
    \label{fig:selfplay}
\end{figure}

\begin{figure}[h]
    \centering
    \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/moe_train_strategies.png}
        \caption{Training strategies for the Hybrid model.}
        \label{fig:moe_train_strategies}
    \end{minipage}
    \hfill
    \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/moe_train_strategies_loss.png}
        \caption{Training strategies for the Hybrid model.}
        \label{fig:moe_train_strategies_loss}
    \end{minipage}
\end{figure}



\input{tables/avg_reward_table_hockey.tex}

\input{tables/performance_opponents.tex}


\subsection{Hybrid Model Evaluation}
The Hybrid model is evaluated with the same setup as the foundation models. The evaluation reward is recorded every 5000 steps (50 epochs) over 20 runs. The average reward is then saved and plotted in \autoref{fig:selfplay}. All other statistics, like critic-, actor-loss and training reward provided in the appendix \autoref{sec:hockey-plots}. The final average evaluation reward over 100 runs is also provided in \autoref{tab:avg_reward}.
In \autoref{fig:moe_train_strategies} we can see that the trainig
